import 'package:flutter/material.dart';

class BoxDecorations {
  static BoxDecoration get defaultDecoration => const BoxDecoration(
      borderRadius: BorderRadius.all(Radius.circular(20)),
      color: Colors.orange);
}
